# -*- coding: utf-8 -*-
# Customer Workspace Dashboard (P1-T17).
#
# Deliberately a self-contained subtree. FPA §7 assigns "KPI widgets, cash
# position, revenue charts, receivable/payable summaries" to the Phase-3 module
# ncollection_account_dashboard; the financial providers here are interim and
# marked for handoff to #119 (F3-T01). Keeping the whole feature in one
# directory makes that handoff a directory move rather than archaeology.
from . import dashboard_data
